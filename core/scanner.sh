#!/usr/bin/env bash
# core/scanner.sh - Filesystem skill discovery and catalog indexing

# Helper to scan directories for skills
scan_skill_directories() {
    local link_dir="${1:-}"
    local source_dir="${2:-}"
    
    # Python helper invoked from catalog.sh
}
